name: Roster_pro
description: Roster Pro v6.90 - 排更日曆 支援真正寫入Google伺服器、自定日曆名稱、班次津貼核實、假期顯示、整月JPG截圖、模式同名檢查、去重同步、全部備份
publish_to: 'none'
version: 6.90.0+90

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  shared_preferences: ^2.3.3
  file_picker: ^8.0.0
  path_provider: ^2.1.5
  device_calendar: ^4.3.3
  timezone: ^0.9.2
  permission_handler: ^11.3.1
  # --- Google 日曆真正寫入，解決三星空名問題 ---
  google_sign_in: ^6.2.1
  googleapis: ^13.2.0
  extension_google_sign_in_as_googleapis_auth: ^2.0.12
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
