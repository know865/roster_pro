name: roster_pro
description: Roster Pro v6.49 - 排更日曆 支援自定日曆名稱直接寫入Google日曆、班次津貼核實、假期顯示、整月JPG截圖、模式同名檢查
publish_to: 'none'
version: 6.49.0+49

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  shared_preferences: ^2.3.3
  file_picker: ^8.0.0
  path_provider: ^2.1.5
  device_calendar: ^4.3.2
  timezone: ^0.9.2
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
